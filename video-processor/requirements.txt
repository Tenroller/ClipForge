# VideoHelper Requirements - Optimized Installation
# Cleaned up to remove unused dependencies and reduce Docker image size

# =============================================================================
# CORE REQUIREMENTS (Always needed)
# =============================================================================
# Web server & API
uvicorn
fastapi
starlette
pydantic
python-multipart

# Video processing (core)
moviepy
imageio
imageio-ffmpeg
opencv-python
numpy
scipy
matplotlib

# Audio/TTS (core)
soundfile
librosa>=0.10.0  # Audio analysis for speaker detection
webrtcvad>=2.0.10  # Voice activity detection

# Data & utils (core)
requests
python-dotenv
termcolor
tqdm
psutil
Pillow
aiofiles

# Logging
structlog

# Authentication & security
PyJWT
bcrypt==4.2.0

# Basic TTS support
espeakng_loader

# Database support
prometheus-client
SQLAlchemy
psycopg2-binary

# =============================================================================
# AI/ML DEPENDENCIES (For AI-powered video generation)
# =============================================================================

google-genai
ultralytics
mediapipe  # For face detection in podcast clips workflow

# Enhanced ASR for better word-level timestamps
stable-ts
faster-whisper
whisper-timestamped
pysubs2
ffmpeg-python

# TTS pipeline (advanced)
kokoro

# =============================================================================
# BRAINROT DEPENDENCIES (For TikTok-style compilations)
# =============================================================================
# Video downloading and processing
yt-dlp @ git+https://github.com/yt-dlp/yt-dlp.git@master
scenedetect

# ML dependencies for brainrot
--extra-index-url https://download.pytorch.org/whl/cu126
torch
torchaudio
torchvision

# =============================================================================
# MONEYPRINTER EXTRAS (For advanced text-to-video features)
# =============================================================================
# Subtitle processing
srt_equalizer

# =============================================================================
# DEVELOPMENT & TESTING (Optional - not needed in production)
# =============================================================================
# Testing (only needed for running tests locally)
pytest
httpx

# Observability (optional - only if SENTRY_DSN is configured)
sentry-sdk

# =============================================================================
# INSTALLATION INSTRUCTIONS
# =============================================================================

# Full installation (all features):
# pip install -r requirements.txt

# Production installation (exclude dev dependencies):
# pip install -r requirements.txt && pip uninstall -y pytest httpx sentry-sdk

# Note: The following dependencies have been REMOVED as unused:
# - g4f, curl_cffi, undetected_chromedriver (web scraping - not used)
# - transformers (~2GB - not used, saves significant space)
# - spacy, en_core_web_sm (NLP models - not used)
# - redis, rq (queue system - we use in-memory queue)
# - flask, flask-cors (legacy MoneyPrinter - not used in current FastAPI app)
# - rich, proglog, colorama, anyio (indirect deps or not directly used)
# - playwright (replaced with Pillow for text rendering)

