# ClipForge Backend Requirements
# Slim API server — video processing deps live in video-processor/requirements.txt

# Web server & API
uvicorn
fastapi
starlette
pydantic
python-multipart

# HTTP clients
requests
httpx

# Data & utils
python-dotenv
termcolor
tqdm
psutil
Pillow
aiofiles
numpy

# Logging
loguru

# Authentication & security
PyJWT
bcrypt==4.2.0

# Database & metrics
prometheus-client
SQLAlchemy
psycopg2-binary

# YouTube metadata extraction (no download — download logic is in video-processor)
yt-dlp @ git+https://github.com/yt-dlp/yt-dlp.git@master

# Development & testing
pytest
sentry-sdk
