AI Video Creator – Frontend

React + Vite + TypeScript + Tailwind + Shadcn UI frontend for unified workflows.

Install
```bash
cd cat-video-creator/frontend
npm install
```

Run
```bash
npm run dev
```

The app expects the backend at `http://localhost:8080`.

